# to ignore files that dockerfile will ignore
# basically the same as .gitignore

# excluding node_modules folder if exists in any path
# this is done to avoid installin devDependencies
*/node_modules

# to ignore git files
.git

# to ignore the public files on server
# these are generated by the npm build command on the Dockerfile
server/public
